Screen {
    layout: vertical;
}

#main-container {
    height: 1fr;
    layout: vertical;
    padding: 0 1;
}

ConfigPanel {
    height: auto;
    margin-bottom: 1;
}

TimesheetTable {
    height: 1fr;
    min-height: 10;
    border: solid $accent;
}

TimesheetTable.hidden {
    display: none;
}

CalendarView {
    height: 1fr;
    min-height: 10;
    border: solid $accent;
    display: none;
}

CalendarView.visible {
    display: block;
}

SummaryPanel {
    height: auto;
    margin-top: 1;
}

#log-panel {
    height: 10;
    min-height: 6;
    max-height: 20;
    margin-top: 1;
    border: solid $accent;
    display: block;
}

#log-panel.hidden {
    display: none;
}
